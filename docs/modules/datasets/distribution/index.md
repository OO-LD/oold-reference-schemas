---
hide:
  - toc
---

# Distribution

One file of a dataset: where it is, in which format, and, where it is tabular, what its
columns mean.

{{ oold_schema_meta_data("datasets", "Distribution") }}

{{ oold_schema_renderer("datasets", "Distribution") }}

The table schema is embedded rather than referenced, because it describes this file and has no
life of its own. A cell or a protocol is the opposite case and is referenced by IRI.
